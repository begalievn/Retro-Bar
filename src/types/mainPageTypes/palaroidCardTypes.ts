export interface PalaroidCardTypes {
  image?: string;
  title: string;
  description: string;
  date: string;
}

export interface PalaroidCardImageType {
  id: number;
  url: string;
  created: string;
}
